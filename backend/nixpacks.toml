# Kalvi Backend — Nixpacks config
# Railway: New Service → GitHub Repo → Akz06/Kalvi → Root Directory: backend

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]
